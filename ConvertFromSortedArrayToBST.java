import javax.swing.tree.TreeNode;

public class ConvertFromSortedArrayToBST {
	/*public TreeNode sortedArrayToBST(int[] nums) {
        return form(nums,0,nums.length-1);
    }
   	public TreeNode form(int[] nums,int start,int end){
        if(start>end)
            return null;
        int mid = (start+end)/2;
       TreeNode root = new TreeNode(nums[mid]);
       root.left = form(nums,start,mid-1);
       root.right = form(nums,mid+1,end);
       return root;
    }*/
  }
